"""Uncertainty Quantification (UQ) package.

Additive layer of UQ methods on top of the existing point-regression model.
Each method reuses the same trained regression model and is compared on the
same metrics (PICP@90/95, MPIW@90/95). See README.md in this folder.
"""
